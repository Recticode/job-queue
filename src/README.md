# Job Queue

This job queue allows multiple workers to process the same job at the same time due to a race condition.

Fix the bug!

Run `recticode start job-queue` in your terminal to start the challenge.